# PakAssist Multimodal RAG — minimal dependencies
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4
PyMuPDF>=1.23.0
Pillow>=10.0.0
google-genai>=0.3.0
python-dotenv>=1.0.0
numpy>=1.24.0

# only needed for backend/graph/graph.py (illustrative wiring example) —
# the real repo already has this
langgraph>=0.1.0

# testing
pytest>=7.4.0
